# Build Outputs
.next/

# Local env files
.env*
!.env*.example
# The end-to-end run only holds dummy credentials — every service it names is intercepted by MSW.
!.env.e2e

# Next.js
next-env.d.ts

# Sanity
# sanity.types.ts

# Lighthouse
# `.lighthouseci` is where `lhci` keeps the raw runs, `lighthouse-report` the reports it renders.
.lighthouseci/
lighthouse-report/

# Playwright
playwright-report/
test-results/

# Vercel
.turbo/
.vercel/
