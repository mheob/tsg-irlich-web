---
import { CounterGroup } from '@/components/items';
import { getStatsSection } from '@/lib/sanity/queries/pages/home';

const { stats } = await getStatsSection();
---

<CounterGroup values={stats} />
