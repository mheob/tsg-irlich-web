---
import ArrowButton from './ArrowButton.astro';

interface Props {
	size?: string;
}

const { size = '48' } = Astro.props;
---

<ArrowButton direction="left" size={size} variant="ghost" />
<ArrowButton class="ml-4" direction="right" size={size} variant="secondary" />
