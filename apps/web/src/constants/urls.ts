/**
 * The base URL for Google Maps search queries.
 * Append an address or location string (URL-encoded) to this constant to generate a Google Maps search link.
 *
 * Example:
 *   `${GOOGLE_MAPS_URL}${encodeURIComponent('Some Address, City, Country')}`
 */
export const GOOGLE_MAPS_URL = 'https://www.google.com/maps/search/?api=1&query=';
