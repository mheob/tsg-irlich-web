{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"extends": ["github>mheob/renovate-config"],
	"schedule": ["before 4am on Thursday"],
	"packageRules": [
		{
			"description": "jsdom 30.1 moved the Blob internals that Vitest's URL.createObjectURL patch reads into a private field, which breaks every upload test on Vitest 4 and 5 alike. Drop this rule once vitest-dev/vitest#11336 is fixed (WEB-318).",
			"matchPackageNames": ["jsdom"],
			"allowedVersions": "<30.1.0"
		}
	]
}
